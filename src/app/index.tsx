import React from 'react';
import ReactDOM from 'react-dom/client';
import { PlazaPage } from '@/pages/plaza/index';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PlazaPage />
  </React.StrictMode>,
);
